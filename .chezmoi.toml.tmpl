[data]
    isWork = {{ promptBoolOnce . "isWork" "Work machine" }}
    email = "{{ promptStringOnce . "email" "Email" "thebriankwon@gmail.com" }}"

[git]
    autoCommit = true
    autoPush = true

[merge]
    command = "nvim"
    args = [
        "-d",
        {{ printf "%q" "{{ .Destination }}" }},
        {{ printf "%q" "{{ .Source }}" }},
        {{ printf "%q" "{{ .Target }}" }},
    ]
